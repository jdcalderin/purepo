{
  "name": "mural-de-pau",
  "version": "1.0.0",
  "private": true,
  "description": "Mural de cumpleanos para Pau con mensajes, fotos y codigo QR.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check worker.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
